FUNCTION verifica_facturados
	PARAMETERS QCOMANDA
	STORE 1 TO XFALTAN
	SELECT * FROM detalle_comanda WHERE pagado=0 AND COMANDA=QCOMANDA AND EMPTY(POWER) INTO CURSOR pendientesfac
	
	IF !EOF()
		XFALTAN=1
	ELSE
		XFALTAN=0
	ENDIF
	
	RETURN XFALTAN
ENDFUNC