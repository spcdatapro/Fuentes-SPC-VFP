SELECT IIF(ENVIADO,"** "+ALLTRIM(A.DESCRIPCION),a.DESCRIPCION) ,ALLTRIM(STR(INT(B.CANTIDAD))),ALLTRIM(STR(SILLA)),b.producto , B.DETALLE_COMANDA FROM PRODUCTO_MENU a, detalle_comanda b where b.comanda = xqcomanda and a.producto=b.producto AND !DETALLE UNION SELECT IIF(ENVIADO,"** "+ALLTRIM(A.DESCRIPCION),a.DESCRIPCION) ,ALLTRIM(STR(INT(B.CANTIDAD))),ALLTRIM(STR(SILLA)),b.producto , B.DETALLE_COMANDA FROM DETALLE_PRODUCTO a, detalle_comanda b where b.comanda = xqcomanda and a.DETALLE_producto=b.producto AND DETALLE INTO CURSOR MICOMANDA